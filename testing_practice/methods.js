var multiply = (first, second) => first * second

// var powerOf = (base, exponent) => {
//   var res = multiply(base, base)
//   for (var step = 0; step < (exponent - 2); step++) {
//     res = multiply(res, base)
//   }
//   return res
// }

module.exports = {
  multiply
// powerOf
}
